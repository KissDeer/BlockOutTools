# 模块出入口

按普通积木放置、旋转和删除。位置对应模块实际连接点，箭头局部 +X 指向模块外部；零度向网页右侧。width 是通行宽度，depth 是箭头显示长度，单位 cm。

外部连接以实例 ID + 出入口 ID 区分占用；复用模块不会占用另一实例的端口。出入口不区分进出，连接类型决定单向或双向语义。

箭头不是可通行地面，也不自动挖墙洞。三维组装使相连端口面对面并应用连接距离；二维折点只改变连线路径。端口不进入 UE Actor 计划。
