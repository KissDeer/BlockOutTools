# LayoutTools 用户手册

本手册面向 LayoutTools 的日常使用者，重点说明功能、使用流程和最佳实践。文档不介绍实现原理。

## 1. 产品定位

LayoutTools 是一款用于 2D 平面布局、关卡设计和多楼层空间预览的工具。它适合用于：

- 绘制建筑平面图、房间布局、走廊、墙体和楼梯。
- 制作游戏关卡草图、3C 角色通行验证、实体点位规划。
- 将 2D 布局实时转换为 3D 预览。
- 使用 AI 根据文字或图片生成空间布局。
- 导出项目数据、2D 截图或 3D 模型，供后续软件使用。

推荐把 LayoutTools 当成“可交互的布局白板 + 3D 预检查工具”使用：先快速建立可用结构，再逐步校准尺寸、层高、门窗、楼梯和导出设置。

## 2. 界面总览

LayoutTools 的主界面分为四个区域：

- 左侧工具栏：选择、绘制、门窗、旋转、楼梯、实体、路网、测量、Polygon 编辑、导入图片、切割、导出选区、撤销重做和网格开关。
- 中央画布：主要编辑区域，可在 2D 和 3D 视图之间切换。
- 顶部栏：关卡名称、缩放比例、2D/3D 切换、深浅色主题切换；当选中两个形状时会出现布尔运算按钮。
- 右侧面板：设置、图层、AI 三个标签页。

常用工作节奏：

1. 在右侧设置基础参数，如网格、尺寸显示、门窗高度、导出单位。
2. 在图层面板创建楼层或分组。
3. 用左侧工具绘制空间。
4. 选中对象，在属性面板中调整名称、颜色、墙厚、楼梯参数、实体类型等。
5. 切换到 3D 检查层高、墙体、门窗和楼梯。
6. 导出 JSON、FBX、GLTF、OBJ 或 PNG。

## 3. 基本操作

### 3.1 启动与运行

LayoutTools 是一款在线工具，无需本地安装。直接访问以下地址即可使用：

**[https://kluiyao.itch.io/layout-tools](https://kluiyao.itch.io/layout-tools)**

在浏览器中打开上述链接，即可进入 LayoutTools 主界面开始绘制。

### 3.2 命名关卡

顶部栏左侧输入框是关卡名称。建议在开始绘制前命名，例如：

- `Office_Blockout_v01`
- `Villa_2F_Concept`
- `Dungeon_Path_Test`

最佳实践：

- 名称会影响导出文件名，建议避免使用过长名称。
- 版本号建议写进名称，便于保留多个阶段导出。
- 团队协作时使用统一命名规则，例如 `项目_区域_版本`。

### 3.3 2D 与 3D 视图

顶部栏和右侧设置面板都可以切换 2D/3D。

- 2D 视图用于绘制、选择、编辑、测量、导入图片和 AI 结果落地。
- 3D 视图用于检查层高、墙体、楼梯、门窗洞口、顶盖和整体空间关系。

最佳实践：

- 绘制时以 2D 为主，完成一个房间组或楼层后切 3D 检查。
- 多楼层项目不要等全部画完才看 3D，应每完成一层就检查一次。
- 导出 3D 前先在 3D 视图确认墙体渲染、楼梯连接和层高关系。

### 3.4 深色与浅色主题

顶部栏的太阳/月亮按钮用于切换主题。主题设置会保存在浏览器本地。

建议：

- 长时间编辑或暗色模型预览时使用深色主题。
- 截图、展示和打印前可切到浅色主题，确保可读性。

## 4. 画布导航

### 4.1 缩放

- 鼠标滚轮：以鼠标位置为中心缩放。
- 触屏双指缩放：在平板或触屏设备上缩放画布。

最佳实践：

- 绘制大体布局时保持较低缩放，便于控制整体比例。
- 放置门窗、编辑顶点和切割时放大视图，减少误点。

### 4.2 平移

- 中键拖动：平移画布。
- `Alt + 左键拖动`：平移画布。
- 触屏双指拖动：平移画布。
- 在图层分离视图中，直接拖动即可平移。

注意：

- Polygon 工具中 `Alt` 用于添加顶点，因此该模式下不要依赖 `Alt + 拖动` 平移。
- 选择工具中，如果已有选中对象，`Alt` 可能用于选择移动锚点。

### 4.3 坐标与单位

画布左下角显示当前鼠标坐标和形状数量。LayoutTools 的编辑单位以厘米为主：

- 图层高度单位：cm。
- 墙厚、门高、窗高、窗台高度单位：cm。
- 面积显示：cm²。
- 导出时可在右侧设置导出比例和目标单位。

最佳实践：

- 一开始确认项目的尺度约定，不要中途改变网格单位含义。
- 建筑类项目建议让网格对应固定模数，例如 50cm 或 100cm。
- 游戏类项目建议先确认引擎单位换算，再设置导出比例。

## 5. 快捷键

| 操作 | 快捷键 |
| --- | --- |
| 撤销 | `Ctrl+Z` |
| 重做 | `Ctrl+Y` |
| 剪切选中对象 | `Ctrl+X` |
| 复制选中对象 | `Ctrl+C` |
| 粘贴对象 | `Ctrl+V` |
| 删除选中对象 | `Delete` / `Backspace` |
| 取消画布旋转并回到选择工具 | `Escape` |
| 退出 Polygon 工具 | `Escape` |
| 切割工具中清除第一个切割点 | `Escape` |
| AI 输入框发送 | `Enter` |
| AI 输入框换行 | `Shift+Enter` |

编辑辅助键：

- `Shift`：在多个操作中用于添加选择、旋转自由/步进控制、顶点对齐等。
- `Ctrl` / `Cmd`：多选图层、复制粘贴、自由移动/缩放、删除顶点、操作锁定图片等。
- `Alt`：画布平移、Polygon 添加顶点、选择工具中的移动锚点选择。
- `V`：移动时启用顶点吸附辅助。

当路点被选择时，剪切、复制、粘贴和删除快捷键同样适用；复制多个路点时，它们之间的路网连线会一起保留。

最佳实践：

- 输入框、聊天框和下拉框聚焦时，快捷键通常不会拦截文本输入。
- 删除、剪切和粘贴前先确认当前选区，尤其是多选状态。
- 使用 `Ctrl+C` / `Ctrl+V` 复制一组对象时，建议先检查它们是否位于正确图层。

## 6. 选择与对象编辑

### 6.1 选择对象

使用左侧第一个选择工具。

常用方式：

- 单击对象：选择形状或实体。
- 框选：在空白画布拖出选择框。
- `Shift` 框选：把框选结果加入当前选区。
- 双击组合内对象：深入选择布尔组合中的非主对象。

最佳实践：

- 复杂场景里先隐藏或锁定无关图层，减少误选。
- 如果对象重叠，先切换图层可见性或放大后再选择。
- 布尔组合对象中，双击进入更细粒度选择后再调局部元素。

### 6.2 移动、缩放、旋转

选中对象后可以在画布上拖动移动、使用控制柄缩放或旋转。

辅助键：

- 移动时按 `Ctrl`：更自由地移动，不按网格吸附。
- 移动时按 `V`：启用顶点吸附。
- 缩放时按 `Ctrl`：自由缩放，不按网格吸附。
- 缩放时按 `Alt`：围绕中心缩放。
- 缩放时按 `Shift`：保持宽高比例或辅助约束。
- 旋转时默认按设置的旋转步长吸附；按 `Shift` 可自由旋转。

最佳实践：

- 初期布局用网格吸附保证整齐；精修阶段再用 `Ctrl` 做局部微调。
- 旋转房间或墙体前，先确认门窗标记是否需要重新校准。
- 复制对象后立即移动到目标位置，避免重叠对象被误认为没有复制成功。

### 6.3 属性面板

单选对象时，左下角会显示属性面板。

形状常用属性：

- 标签：房间名或区域名。
- 面积、宽度、高度、半径。
- 填充颜色。
- 内描边宽度和颜色。
- 墙体厚度。
- 墙体高度覆盖。
- 门窗标记重置。
- 楼梯参数。
- 层级排序。
- 图片透明度、锁定和移除。

实体常用属性：

- 实体类型。
- 缩放比例。
- 层级排序。

最佳实践：

- 每个房间都建议填写标签，AI 合并、门窗生成和团队沟通都会更清楚。
- 对可走空间使用稳定命名，例如 `客厅`、`走廊`、`主卧`，不要频繁改名。
- 图片参考底图完成描绘后建议锁定，避免误拖动。

### 6.4 多选属性

多选形状后会显示多选属性面板。

可批量调整：

- 墙体厚度。
- 内描边宽度。
- 内描边颜色。
- 填充颜色。

最佳实践：

- 创建同一楼层墙体后，用多选统一墙厚。
- 为不同功能区设置统一颜色，后续在 3D 和导出中更容易辨认。
- 批量调整前先排除不该修改的对象，例如图片底图和实体。

## 7. 左侧工具栏

左侧工具栏中，带小三角的按钮表示包含子工具。短按会使用默认或当前子工具；长按约 300ms 可展开子工具菜单。

### 7.1 选择工具

用途：

- 选择对象和可见路点。
- 移动、缩放、旋转对象。
- 框选多个对象或路点。
- 移动、剪切、复制、粘贴、删除选中内容。
- 调整组合内对象。

最佳实践：

- 完成绘制后立即切回选择工具，避免误继续绘制。
- 编辑复杂布局前先放大目标区域。
- 如需用选择工具编辑路点，先确认“显示路网”已开启；选择任一路网工具时，路网会临时显示。

### 7.2 绘制形状：矩形、圆形、墙体

绘制工具组包含：

- 矩形：适合房间、区域、平台、楼梯占位。
- 圆形：适合圆形空间、塔楼、圆形平台、螺旋楼梯范围。
- 墙体：适合自由折线墙、走廊边界和非矩形墙段。

矩形/圆形使用方式：

1. 选择绘制工具。
2. 在画布按下鼠标。
3. 拖动到目标尺寸。
4. 松开完成绘制。

墙体使用方式：

1. 选择墙体工具。
2. 逐点点击添加墙体中心线顶点。
3. 双击完成墙体。
4. 选中墙体后在属性面板调整墙厚。

墙体编辑：

- 默认拖动墙体顶点可移动顶点。
- `Alt + 点击边`：添加墙体顶点。
- `Ctrl/Cmd + 点击顶点`：删除顶点。
- `Ctrl/Cmd + 点击边`：删除墙段。
- `Shift` 可用于墙体顶点连接或辅助对齐。

最佳实践：

- 房间优先用矩形或 Polygon 编辑生成，复杂边界再用墙体工具。
- 自由墙体先画中心线，再调墙厚，不要一开始就追求完全精确。
- 墙体和房间混用时，为墙体单独放一个图层，便于显示和导出控制。

### 7.3 门、窗、描边还原

门窗工具组包含：

- 门工具。
- 窗工具。
- 描边还原工具。

门窗使用方式：

1. 在右侧设置中确认门色/窗色配置和当前选中色。
2. 选择门或窗工具。
3. 在目标墙体或房间边缘拖出一段。
4. 系统会在对象描边中生成开口标记。

如果已有形状被选中，门窗工具优先作用于选中的形状；否则会尝试作用于命中的形状。

门配置：

- 每种门颜色对应一个门高。
- 默认门高为 210cm。

窗配置：

- 每种窗颜色对应窗台离地高度和窗高。
- 默认窗台高度为 90cm，窗高为 120cm。

描边还原：

- 用于修复或还原被门窗切掉的描边段。

最佳实践：

- 同一类门窗使用同一颜色，避免导出后难以识别。
- 多种门高时先在设置中建立不同颜色，再放置门。
- 放门窗前确认墙体或房间的内描边/墙体厚度，否则开口效果可能不明显。
- AI 自动生成布局后，先检查门窗是否合理，再手动微调关键门洞。

### 7.4 画布旋转

画布旋转工具用于旋转 2D 编辑视角，不改变实际对象坐标。

使用方式：

1. 选择画布旋转工具。
2. 在画布拖动旋转。
3. 顶部和画布中央会显示角度与指南针。
4. 使用旋转工具组里的重置按钮，或按 `Escape` 回到 0° 并切回选择工具。

最佳实践：

- 用于编辑斜向建筑或斜向关卡区域。
- 导出前建议重置画布旋转，避免视觉判断和实际坐标混淆。

### 7.5 楼梯工具

楼梯工具组包含：

- 直梯。
- 回旋梯。
- 螺旋梯。

楼梯本质上是带楼梯属性的形状。绘制后选中楼梯，可在属性面板调整：

通用属性：

- 楼梯类型。
- 楼梯模式：斜面或阶梯。
- 关联目标图层。
- 关联边。
- 地板挖洞模式：上层、下层、上下、不挖。

直梯属性：

- 水平/垂直方向。
- 直梯段数。
- 多段时可调整楼梯井间距。

回旋梯属性：

- 直梯段数。
- 楼梯井大小。

螺旋梯属性：

- 起始角度。
- 结束角度。
- 楼梯井大小。
- 是否中空。

最佳实践：

- 先建立所有楼层图层，再画楼梯并设置目标图层。
- 楼梯应跨越两个不同高度的图层，否则 3D 中无法体现上下关系。
- 多楼层项目建议打开图层分离视图检查楼梯箭头。
- 先用简单直梯验证层间连接，再替换成复杂回旋或螺旋形式。

### 7.6 实体工具

实体工具用于在平面中放置游戏或交互点位。

内置实体类型：

- 玩家起点。
- 敌人。
- 拾取物。
- 触发器。
- 灯光。
- 道具。
- 楼梯。
- 角色。

使用方式：

1. 选择实体工具。
2. 在画布点击放置实体。
3. 选中实体，在属性面板中修改类型和缩放。

角色实体：

- 角色实体使用 3C 胶囊参数。
- 可在右侧设置中调整名称、半高、半径、速度和跳高。

自定义实体：

- 在右侧设置的实体图标配置中添加自定义实体类型。
- 可为内置或自定义实体上传图标图片。

最佳实践：

- 游戏关卡中先放玩家起点和关键目标点，再补充敌人、触发器和道具。
- 用角色实体测试通行空间时，先设置正确胶囊半径。
- 自定义实体类型命名建议使用英文或稳定 ID，如 `npc_vendor`、`cover_low`。

### 7.7 路网工具

路网工具用于维护道路节点和有向连接，常用于多建筑、街区、村镇或园区布局中的入口连通关系。AI 生成多建筑场景时，也可能自动生成路点和路网连线。

路网工具组包含：

- 路点工具：创建、插入或拖动路点。
- 关联路点：在两个路点之间创建有向连线。
- 删除关联/路点：删除路点或路网连线。

使用方式：

1. 选择路点工具，在画布空白处点击创建路点。
2. 用路点工具点击已有连线，可在连线上插入新路点并拆分连线。
3. 用路点工具拖动已有路点，可移动路点位置。
4. 选择关联路点，先点击源路点，再点击目标路点，创建带方向的连线。
5. 选择删除关联/路点，点击路点会删除该点及其相关连线，点击连线则只删除该连线。

选择工具中的路点编辑：

- 路网可见时，可用选择工具单击或框选路点。
- 选中路点后可以拖动移动，也可以使用 `Ctrl/Cmd+X`、`Ctrl/Cmd+C`、`Ctrl/Cmd+V` 和 `Delete` / `Backspace`。
- 复制多个路点时，所选路点之间的连线会一起复制；粘贴后会生成新的路点和对应连线。

显示规则：

- 右侧设置中的“显示路网”默认开启。
- 关闭“显示路网”后，普通选择和编辑状态下不显示路网，选择工具也不会命中隐藏路点。
- 选择路点工具、关联路点或删除关联/路点时，即使没有勾选“显示路网”，系统也会临时显示路网，方便编辑。

最佳实践：

- 路网方向建议从公共空间指向私密空间，例如广场或主路 → 道路转折点 → 建筑入口。
- 路点尽量贴合道路中心线，终点可放在建筑内或靠近道路一侧。
- 使用“重新计算门窗”前，先检查连线是否穿过目标建筑外墙。

### 7.8 测量工具

测量工具组包含：

- 尺子：直接测量两点距离。
- 步行测量：按可通行路径计算距离。

尺子使用方式：

1. 选择尺子工具。
2. 点击或拖动设置起点和终点。
3. 画布显示测量结果。

步行测量使用方式：

1. 选择步行测量工具。
2. 设置起点。
3. 设置终点。
4. 系统尝试计算可达路径；不可达时显示不可达提示。

最佳实践：

- 用尺子验证尺寸，用步行测量验证玩家路径。
- 放置门后再做步行测量，否则路径可能被墙体阻断。
- 多楼层通行要结合楼梯和图层高度一起检查。

### 7.9 Polygon 编辑

Polygon 编辑工具组包含：

- Polygon 编辑：统一模式。
- 添加顶点。
- 删除顶点。
- 移动顶点。

统一模式快捷操作：

- 默认拖动：移动顶点。
- `Alt + 点击边`：添加顶点。
- `Ctrl/Cmd + 点击顶点`：删除顶点。

顶点移动辅助：

- 拖动顶点时按 `Shift`：按 X 方向对齐目标。
- 拖动顶点时按 `Ctrl`：按 Y 方向对齐目标。
- 同时使用辅助键时可更精确地对齐其他顶点或边。

注意：

- Polygon 编辑会自动打开 Polygon 显示。
- 退出 Polygon 工具时，如果不是手动打开的 Polygon 显示，系统会自动关闭。
- 原始顶点的编辑可能作为视觉偏移保存，不一定等同于把基础形状改成自由路径。

最佳实践：

- 先用矩形画房间，再用 Polygon 添加少量顶点生成凹凸形状。
- 不要一开始就用很多顶点堆复杂轮廓，后续门窗和导出会更难检查。
- 顶点密集时放大操作，并用 `Shift`/`Ctrl` 做对齐。

### 7.10 导入图片

导入图片工具可把图片作为矩形形状放到画布中心，常用于临摹底图。

使用方式：

1. 点击左侧导入图片按钮。
2. 选择图片文件。
3. 图片会缩放到合适大小并放在当前视图附近。
4. 选中图片后可调透明度、锁定或移除。

锁定图片：

- 锁定后，需要按住 `Ctrl` 才能选择或变换图片。
- 适合作为描图底板。

最佳实践：

- 导入后先调透明度到 30%-60%。
- 调整到正确位置后立即锁定。
- 建议把底图放在单独图层，便于隐藏和删除。

### 7.11 切割工具

切割工具用于沿两个顶点之间的线切分形状。

使用方式：

1. 选择切割工具。
2. 点击第一个顶点。
3. 点击第二个顶点。
4. 系统尝试将形状切割成两个部分。

辅助：

- 按 `Escape` 可清除第一个切割点。
- 如果没有选中第一个点，再按 `Escape` 会退出切割工具。
- 第二点选择时可配合 `Shift` 做辅助约束。

最佳实践：

- 切割前最好先复制一份对象备用。
- 只对边界清晰的形状使用切割，复杂布尔组合建议先导出为独立对象或整理组合。
- 切割后立即检查面积和门窗标记是否仍符合预期。

### 7.12 导出选区为图片

左侧相机按钮可把当前选中对象导出为 PNG。

使用方式：

1. 选中一个或多个对象。
2. 点击导出选区按钮。
3. 系统下载 `selection_时间戳.png`。

最佳实践：

- 用于分享局部方案、楼梯详图或房间组。
- 导出前隐藏无关图层，减少干扰。
- 图片底图和实体图标也会尽量绘制到导出结果中。

### 7.13 撤销、重做、网格

左侧底部包含：

- 撤销。
- 重做。
- 网格显示开关。

历史记录上限为 50 步。

最佳实践：

- 大规模改动前导出 JSON 备份，而不是只依赖撤销历史。
- 关掉网格适合截图，打开网格适合编辑。

## 8. 布尔运算

当选中两个形状时，顶部右侧会出现布尔运算按钮：

- 并集：合并两个形状区域。
- 交集：保留重叠区域。
- 差集：从主形状中减去目标形状。

布尔结果以组合和修改器形式保存，可在属性面板中管理：

- 查看修改器列表。
- 启用或禁用修改器。
- 调整修改器顺序。
- 删除修改器。
- 导出为独立对象。

最佳实践：

- 用布尔运算处理门厅、凹室、异形空间时很高效。
- 复杂组合建议最后“导出为独立对象”，减少后续编辑歧义。
- 布尔前确保两个对象位于同一逻辑图层或同一功能区域。
- 差集顺序很重要，先选主对象再选用于扣减的对象。

## 9. 图层管理

右侧图层面板用于管理楼层、分组、可见性、锁定和 3D 墙体关系。

### 9.1 图层基础

每个图层包含：

- 名称。
- 可见/隐藏。
- 锁定/解锁。
- 高度。
- 颜色。
- 分组。
- 3D 墙体开关。
- 墙顶目标层。
- 盖顶开关。

操作：

- 点击加号新建图层。
- 点击文件夹加号新建图层分组。
- 点击眼睛切换可见性。
- 点击锁切换锁定。
- 双击已选图层名称编辑名称。
- 拖动图层到分组。
- 使用上下按钮调整图层顺序。
- 删除图层会同时删除该图层上的形状和实体。

最佳实践：

- 每个楼层至少一个图层，例如 `1F`、`2F`、`Roof`。
- 图层高度按真实垂直高度填写，例如 0、300、600。
- 底图、标注、墙体、实体可以拆成不同图层，便于隐藏和导出控制。
- 删除图层前先确认其中对象不再需要。

### 9.2 图层分组

图层分组用于管理多个相关图层。

用途示例：

- `建筑主体`：包含 1F、2F、屋顶。
- `参考图`：包含所有导入底图。
- `游戏点位`：包含实体和触发器。

最佳实践：

- 用分组控制复杂项目的可见性。
- 多人协作时，按专业分组，如建筑、关卡、互动、标注。

### 9.3 3D 墙体与墙顶目标层

图层开启 3D 墙体后，系统会根据形状内描边或墙体信息生成 3D 墙面。

墙顶目标层：

- 指定另一个图层作为墙体高度目标。
- 墙高通常等于目标层高度减当前层高度。

盖顶：

- 在墙顶目标高度生成覆盖面或顶面。

最佳实践：

- 多层建筑建议每层墙体目标指向上一层。
- 顶层墙体可设置到屋顶图层，或通过单个形状墙高覆盖控制高度。
- 如果墙太高或太低，先检查图层高度和墙顶目标层，再检查单个形状的墙高覆盖。

### 9.4 图层分离视图

右侧设置中可打开图层分离视图。它会把各图层按高度分开显示，并显示：

- 图层名称和高度。
- 图层分组边框。
- 楼梯连接箭头。
- 墙体目标层箭头。
- 每层上的形状和实体。

操作：

- 滚轮缩放。
- 拖动平移。
- `Escape` 关闭。

最佳实践：

- 多楼层项目每次新增楼梯后都检查一次。
- 检查楼层高度是否排序合理。
- 检查墙体箭头是否指向正确目标层。

## 10. 右侧设置

右侧设置面板包含基础设置、视图设置、门窗配置、角色参数、实体图标、导入导出、导出比例和语言设置。

### 10.1 基础设置

可配置：

- 关卡名称。
- 网格大小。
- 旋转步长。
- 是否显示网格线。
- 是否显示尺寸标注。
- 是否显示路网。
- 是否显示 Polygon 模式。
- 标签字体大小。
- 数据字体大小。

最佳实践：

- 网格大小在项目开始时确定，避免后期整体尺度混乱。
- 旋转步长可设为 15° 或 45°；规则建筑适合 15°，斜向关卡可更小。
- 不编辑路网时可关闭“显示路网”减少画布干扰；切换到路网工具时会临时显示。
- 展示截图前适当调大标签字体。

### 10.2 视图设置

可配置：

- 2D / 3D 切换。
- 图层分离视图。
- 3D 墙体渲染模式：不透明、半透明、Dither、隐藏。
- 墙体透明度。
- 3D 标签显示。
- 3D 线框显示。
- 法线显示。

最佳实践：

- 编辑空间关系时用半透明或 Dither。
- 检查模型结构时打开线框。
- 法线显示主要用于排查模型面方向问题，普通设计流程一般不需要。

### 10.3 门颜色配置

每个门颜色配置包含：

- 颜色。
- 门高。
- 是否为当前选中门色。

可以添加多个门配置。

最佳实践：

- 用颜色区分普通门、矮门、高门、特殊门。
- 门高与目标引擎或建筑规范保持一致。

### 10.4 窗颜色配置

每个窗颜色配置包含：

- 颜色。
- 窗台离地高度。
- 窗高。
- 是否为当前选中窗色。

可以添加多个窗配置。

最佳实践：

- 用颜色区分普通窗、高窗、落地窗。
- 窗台高度加窗高不要超过墙高。
- 如果 3D 中窗洞异常，优先检查该颜色配置的离地高度和窗高。

### 10.5 3C 角色胶囊

可配置：

- 名称。
- 半高。
- 半径。
- 移动速度。
- 跳高。

最佳实践：

- 角色半径用于通行验证，建议按项目最终角色碰撞体设置。
- 步行测量和布局通道宽度应围绕角色胶囊半径规划。
- 如果项目有多个角色体型，可用不同文件或版本保存不同参数。

### 10.6 实体图标配置

可配置：

- 为内置实体上传图标。
- 添加自定义实体类型。
- 为自定义实体上传图标。
- 删除自定义实体类型。

删除自定义类型时，已有该类型实体会回退为道具类型。

最佳实践：

- 自定义类型名使用稳定 ID，不要用临时说明句。
- 图标建议使用透明背景 PNG。
- 导出前检查自定义实体是否都已有清晰图标或类型名。

### 10.7 语言设置

可配置：

- 当前语言。
- 导出语言包。
- 导出翻译模板。
- 导入语言包。

最佳实践：

- 本地化流程建议：导出模板 -> 翻译 -> 导入语言包 -> 检查界面。
- 翻译时不要修改 JSON 键名，只修改文本值。

## 11. AI 助手

AI 面板用于通过自然语言或图片生成、修改布局。

### 11.1 配置 AI

首次使用前需要在 AI 面板中配置模型：

1. 打开右侧 AI 标签。
2. 点击模型设置。
3. 选择服务商。
4. 填入 API Key。
5. 选择或刷新模型列表。

支持的服务商预设包括：

- CodeBuddy CLI。
- 腾讯混元。
- OpenAI。
- Google Gemini。
- DeepSeek。
- Anthropic Claude。
- 硅基流动。
- 通义千问。
- 智谱 AI。
- 小米 MiMo。
- 自定义 OpenAI 兼容端点。

自定义端点支持选择 Chat Completions 或 Responses API 格式。

注意：

- API Key 保存在浏览器本地。
- 如果模型列表刷新失败，可手动输入模型名称。
- 图片输入需要选择支持视觉能力的模型。

### 11.2 文本生成布局

示例提示词：

```text
设计一个 120 平米的三室两厅住宅，包含玄关、客厅、餐厅、厨房、主卧、两个次卧、卫生间和阳台。要求南北通透，卧室集中在右侧，客厅连接阳台。
```

生成后，AI 消息会显示：

- 关卡名称。
- 形状数量和房间名称。
- 实体数量。
- 图层数量。
- 门窗数量。
- 楼梯类型。
- 待生成楼层或分块。

可选择：

- 应用到画布：按 AI 返回坐标应用或合并。
- 应用到当前视角中心：忽略 AI 坐标中心，把结果放到当前视图附近。

最佳实践：

- 一次生成时描述房间列表、相邻关系、楼层数量和风格目标。
- 对平面布局要求强的任务，明确“哪些空间相邻”“入口在哪里”“哪些空间需要外窗”。
- 多楼层建筑要写清每层功能和楼梯位置。
- 如果现有画布已有内容，说明是“替换全部”还是“在现有布局上增加/修改”。

### 11.3 上传图片生成

使用方式：

1. 点击 AI 输入框左侧图片按钮。
2. 上传平面图或参考图。
3. 输入要求，例如“分析这张图并还原成可编辑布局”。
4. 发送。
5. 检查结果并应用。

限制：

- 图片大小不能超过 10MB。
- 图片解析效果取决于模型视觉能力和图纸清晰度。

最佳实践：

- 上传前裁掉无关边框和水印。
- 使用高对比、清晰的平面图。
- 复杂图纸建议分区域上传，逐块生成。

### 11.4 增量修改

可让 AI 修改现有布局，例如：

```text
把客厅扩大 20%，在走廊末端增加一个储物间，并把主卧门改到走廊一侧。
```

最佳实践：

- 使用已有房间标签描述目标，例如“主卧”“走廊”“客厅”，不要说“左边那个房间”。
- 修改前先给房间命名，AI 更容易匹配。
- 重要布局建议应用后手动检查门窗和路径。

### 11.5 多楼层和分块生成

AI 支持多楼层或大型布局的分步生成。当结果中出现待生成楼层或分块时，应用按钮会触发后续自动生成和应用。

最佳实践：

- 多楼层提示词中使用清晰层名，如 `1F`、`2F`、`Roof`。
- 说明楼梯连接哪些楼层。
- 生成后打开图层分离视图检查连接箭头。

### 11.6 AI 提示词建议

好的提示词包含：

- 空间类型：住宅、办公室、地牢、学校、医院、博物馆等。
- 总体规模：大约面积、层数、房间数量。
- 必要房间：明确列出。
- 相邻关系：谁连接谁。
- 出入口位置：入口、阳台、楼梯、电梯等。
- 约束：采光、动线、通行、对称、开放式或封闭式。
- 输出方式：替换现有布局或合并到当前布局。

示例：

```text
在当前布局上增加一个二层。二层包含主卧、书房、卫生间和露台。楼梯从一层客厅东北角连接到二层走廊。保留当前一层，不要替换。
```

## 12. 3D 预览

3D 视图会根据当前布局生成三维预览，包括：

- 房间或区域平面。
- 根据内描边/墙体生成的墙体。
- 门窗洞口。
- 楼梯几何。
- 层高关系。
- 图片贴图。
- 实体图标或角色胶囊。

操作：

- 左键：选择对象。
- 右键：旋转视图。
- 中键：平移。
- 滚轮：缩放。
- 触屏：支持双指缩放和平移。

3D 显示设置：

- 墙体不透明：用于检查实体墙。
- 墙体半透明：用于同时观察内部空间。
- Dither：适合综合观察。
- 隐藏墙体：只看地面与对象。
- 标签：显示或隐藏房间名。
- 线框：检查模型边界。
- 法线：排查模型异常。

最佳实践：

- 导出 3D 模型前先检查门窗是否穿透到正确墙体。
- 多层模型中，确认每层高度、墙体高度和楼梯目标层。
- 如果 3D 中看不到墙，检查图层是否开启 3D 墙体，形状是否有内描边或墙体厚度。

## 13. 导入与导出

### 13.1 JSON 导出/导入

JSON 是完整项目数据格式。

导出：

1. 打开右侧设置。
2. 展开导入/导出。
3. 点击导出。
4. 下载 `关卡名_level.json`。

导入：

1. 打开右侧设置。
2. 展开导入/导出。
3. 点击导入。
4. 选择 `.json` 文件。

JSON 包含：

- 关卡名称。
- 网格设置。
- 图层和分组。
- 形状。
- 实体。
- 门窗配置。
- Polygon 数据。
- 角色参数。
- 实体图标配置。
- 导出比例。

最佳实践：

- 重大修改前导出 JSON 备份。
- 需要跨浏览器或跨电脑迁移项目时优先使用 JSON。
- 不要手动编辑 JSON，除非确认字段含义。

### 13.2 FBX 导出

适合导入部分 DCC 软件或游戏引擎。

导出内容通常包括：

- 3D 场景几何。
- 墙体。
- 楼梯。
- 门窗洞口。
- 当前 Polygon 形变。

最佳实践：

- 导出前在 3D 视图检查模型。
- 确认导出比例和目标单位。
- 复杂布尔对象建议先整理或导出为独立对象。

### 13.3 GLTF 导出

GLTF 适合 Web、Three.js 和现代 3D 管线。

最佳实践：

- Web 预览优先使用 GLTF。
- 如果材质、颜色和层级信息需要保留，优先测试 GLTF。
- 导入目标软件后检查坐标轴和比例。

### 13.4 OBJ 导出

OBJ 适合通用几何交换。

最佳实践：

- 用于只需要模型几何的流程。
- 如果需要复杂材质、层级或动画信息，不建议只依赖 OBJ。

### 13.5 PNG 导出

PNG 通过左侧导出选区按钮完成。

最佳实践：

- 导出局部前先选中目标对象。
- 隐藏无关图层。
- 调整标签字体大小，提高可读性。

### 13.6 导出比例和单位

右侧设置中可定义：

- 多少目标单位等于 1cm。
- 单位类型：mm、cm、m、inch、feet、UU。

系统会显示换算表。

最佳实践：

- Unity/Unreal 等引擎导入前先确认目标单位。
- 建议先导出小测试模型，确认比例正确后再导出完整项目。
- 团队协作中把单位换算写进项目规范。

## 14. 推荐工作流

### 14.1 从零手绘平面图

1. 设置关卡名称、网格大小和旋转步长。
2. 创建必要图层。
3. 用矩形画主要房间。
4. 给每个房间命名。
5. 用 Polygon 编辑微调异形边界。
6. 批量调整颜色和内描边。
7. 配置门窗颜色和高度。
8. 放置门窗。
9. 切换 3D 检查墙体和洞口。
10. 导出 JSON 备份。

### 14.2 用图片临摹

1. 导入参考图。
2. 调整图片大小、位置和透明度。
3. 锁定图片。
4. 新建绘制图层。
5. 沿底图绘制房间和墙体。
6. 完成后隐藏或删除底图。
7. 放置门窗并切 3D 检查。

### 14.3 用 AI 快速生成方案

1. 配置 AI 服务商和模型。
2. 用提示词描述空间、面积、房间、动线和楼层。
3. 生成后阅读摘要。
4. 应用到画布或当前视角中心。
5. 手动修正尺寸、门窗和楼梯。
6. 切 3D 检查。
7. 导出 JSON 保存阶段版本。

### 14.4 多楼层建筑

1. 建立 `1F`、`2F`、`Roof` 等图层。
2. 设置每层高度。
3. 在每层绘制对应空间。
4. 在下层或起始层绘制楼梯。
5. 设置楼梯目标图层。
6. 开启 3D 墙体并设置墙顶目标层。
7. 打开图层分离视图检查箭头。
8. 切 3D 检查整体空间。

### 14.5 游戏关卡验证

1. 绘制可通行区域和阻挡墙体。
2. 放置玩家起点、敌人、道具、触发器。
3. 设置角色胶囊半径和移动参数。
4. 用步行测量检查关键路径。
5. 用 3D 检查垂直关系和楼梯。
6. 导出 FBX/GLTF/OBJ 给引擎测试。

## 15. 最佳实践总表

### 15.1 命名

- 房间和区域尽量都有标签。
- 多楼层房间可加前缀，如 `1F-客厅`、`2F-主卧`。
- 自定义实体类型使用稳定英文 ID。

### 15.2 图层

- 每个楼层独立图层。
- 参考图独立图层。
- 实体和标注可以单独图层。
- 不要把所有内容都放在基础图层。

### 15.3 尺寸

- 开工前确认网格大小。
- 尽量用网格完成大体结构。
- 精修阶段再自由移动。
- 导出前检查单位换算。

### 15.4 门窗

- 先配置门窗颜色和高度，再放置。
- 一种高度对应一种颜色。
- 放置后用 3D 检查洞口。
- AI 生成后务必检查外窗和门洞位置。

### 15.5 楼梯

- 先建目标层，再画楼梯。
- 设置目标层和挖洞模式。
- 用图层分离视图检查连接。
- 用 3D 检查上下层高度和楼梯方向。

### 15.6 AI

- 提示词写清房间列表和相邻关系。
- 先生成粗方案，再分步修改。
- 生成结果应用前看摘要。
- 应用后手动检查门窗、路径和楼梯。

### 15.7 导出

- 导出前保存 JSON。
- 导出 3D 前检查 3D 视图。
- 首次接入引擎时先导出小样验证比例。
- PNG 用于沟通，JSON 用于备份，FBX/GLTF/OBJ 用于模型流转。

## 16. 常见问题

### 16.1 为什么 3D 中没有墙？

检查：

- 当前图层是否开启 3D 墙体。
- 图层是否可见。
- 形状是否有内描边或墙体厚度。
- 墙体渲染模式是否被设置为隐藏。
- 墙顶目标层或单个墙高是否合理。

### 16.2 为什么门窗洞口不明显？

检查：

- 形状是否有足够的内描边宽度或墙厚。
- 门窗是否放在正确形状上。
- 当前门窗颜色配置是否合理。
- 窗台高度加窗高是否超过墙高。

### 16.3 为什么步行测量显示不可达？

可能原因：

- 起点和终点之间没有门洞。
- 墙体或房间边界阻断路径。
- 楼梯没有正确连接目标图层。
- 起点或终点放在不可通行区域。

### 16.4 为什么图片无法编辑？

如果图片被锁定，需要按住 `Ctrl` 才能选择或变换。也可以在属性面板中解锁图片。

### 16.5 为什么复制粘贴没反应？

检查：

- 当前是否有选中对象。
- 焦点是否在聊天框或输入框中。
- 粘贴后对象可能与原对象重叠，需要立即拖动查看。

### 16.6 为什么 AI 不能使用？

检查：

- 是否已填写 API Key。
- 服务商和模型是否正确。
- 自定义端点是否填写 Base URL。
- 图片任务是否选择支持视觉的模型。
- 网络或服务商额度是否正常。

### 16.7 为什么导出的模型比例不对？

检查：

- 右侧导出比例设置。
- 目标软件的导入单位。
- 是否把 cm、m、UU 等单位混用。

### 16.8 为什么删除图层后对象也没了？

删除图层会同时删除该图层上的形状和实体。这是正常行为。删除前建议导出 JSON 备份。

## 17. 维护建议

- 每完成一个阶段导出一次 JSON，例如 `v01_blockout`、`v02_doors`、`v03_3d_check`。
- 复杂项目保留图片底图图层，直到最终确认。
- 团队交付时同时提供 JSON 和目标 3D 格式。
- 在 README 或项目说明中记录网格大小、单位换算、楼层高度和命名规范。
