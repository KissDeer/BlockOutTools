# BlockOutTools 工作区约定

关卡白盒工具：把设计构想变成可分模块搭建、能检查、能导出到 UE 的关卡。

## 开始任何制作前，先读设计目录

**动手前先读 `design-log/INDEX.md`**，按索引找相关条目再读对应文件。这里记录的是"为什么这么做"和已经踩过的坑；需求基线在 `docs/rebuild/`，两者职责不同：

- `design-log/` —— 设计过程与思路。随对话演进、按时间累积、允许推翻自己。
- `docs/rebuild/` —— 需求单一来源。正式、稳定、经确认；改产品行为必须先改这里。

## 每次结束对话前，整理设计目录

1. 把本次的设计过程写进 `design-log/YYYY-MM-DD-主题.md`（同一天多个主题就多开文件）。
2. 更新 `design-log/INDEX.md`：加一行，写清日期、状态、涉及文件、结论摘要。
3. 只记**决策与理由**，不抄代码、不抄 diff。写清"当时有哪些选项、为什么选它、放弃了什么"。

状态用：`提案` / `已实现` / `部分实现` / `已废弃`。已废弃的条目保留并注明被什么取代，不删除。

## 项目事实（避免重复踩坑）

- 主流程是 **逻辑拓扑 ⇄ 拼接搭建** 两个工作面。模块划分在拓扑完成，模块内部形态在搭建中形成。
- **三套坐标不能混**：`graphPosition` 只是画布排版，无语义；**模块局部厘米**是模块内部坐标；**世界坐标**只在整体组装时产生。
- 跨步骤的一致性靠**指纹**：拓扑 / 上下文 / 几何各有 digest，下游产物必须携带它依据的 digest，漂移就拒绝，不静默沿用。
- 积木用 `provenance`（源 + 特征 ID）表达"来自哪个区域或链路"，**不靠名字匹配**。
- 所有候选（识别 / 拆解 / 构型 / 模块草案）都必须**先预览、可取消、采用是一次可撤销事务**。

## 诚实规则（本项目反复强调）

- 不假装看过图：缺原图、比例未标定、方位未知，都要写进 `assumptions`。
- 规范检查抓到的问题照实报，包括自己刚写的草案引入的错误。
- "逻辑连通" ≠ "空间已对接" ≠ "形态已确认" ≠ "玩法已验证"，不要用一个完成标记掩盖。
- 发现自己的旧结论错了，在 `design-log/` 里更正并写明原因，不要悄悄改。

## 常用命令

```bash
cd app-v2
npm run dev        # http://127.0.0.1:4174/
npx tsc -b         # 类型检查（新增 domain 文件要加进 tsconfig.node.json 的 include）
npx vitest run     # 全部测试
```

新增领域文件后若报 TS6307，把它加进 `app-v2/tsconfig.node.json` 的 `include` 数组。

编辑文件时先停掉 dev server：Vite 监听会锁定文件导致写入失败（EBUSY）。
