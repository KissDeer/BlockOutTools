# BlockOutTools 工作区约定

关卡白盒工具：把设计构想变成可分模块搭建、能检查、能导出到 UE 的关卡。

## 开始任何制作前

**先读 `work-log/NOW.md`** —— 一页纸，写着上次干到哪、接下来干什么、当前状态和别忘的坑。要做的事通常就在"接下来干什么"里。

不在的话再读 **`design-log/INDEX.md`**，按索引找相关设计条目。

三套记录分工不同，别混：

- `work-log/` —— 进度与交接。回答"**到哪儿了**"。一页纸，频繁改写。
- `design-log/` —— 设计过程与思路。回答"**为什么这么做**"。按主题累积，废弃的也保留。
- `docs/rebuild/` —— 需求单一来源。正式、稳定；改产品行为必须先改这里。

## 每次结束对话前

1. **更新 `work-log/NOW.md`**（必做）：改"上次做到哪"和"接下来干什么"，勾掉做完的。
2. 有设计决策 → 写 `design-log/YYYY-MM-DD-主题.md`，并在 `design-log/INDEX.md` 加一行。
3. 有值得下次知道的经过 → 写 `work-log/YYYY-MM-DD.md`；没有就不写。

只记**决策与理由**和**进度**，不抄代码、不贴 diff、不复述提交信息。设计条目要写清"当时有哪些选项、为什么选它、放弃了什么"。

状态用：`提案` / `已实现` / `部分实现` / `已废弃`。已废弃的条目保留并注明被什么取代，不删除。

## 项目事实（避免重复踩坑）

- 主流程是**一张画布**：逻辑层画拓扑，叶子层画几何，进入节点 = 换焦点。没有顶层页签，层级树与面包屑说明"我在哪"。
- **三套坐标不能混**：`graphPosition` 只是画布排版，无语义；**节点局部厘米**是节点内部坐标；**世界坐标**只在整体组装时产生。
- 跨步骤的一致性靠**指纹**：拓扑 / 上下文 / 几何各有 digest，下游产物必须携带它依据的 digest，漂移就拒绝，不静默沿用。
- 积木用 `provenance`（源 + 特征 ID）表达"来自哪个区域或链路"，**不靠名字匹配**。
- 所有候选（识别 / 模块草案）都必须**先预览、可取消、采用是一次可撤销事务**；采用前给出规则检查的试算影响。
- 节点正在成为唯一的容器（`blocks` + `childScopeId`），模块层在削除中。改动前先看 `work-log/NOW.md` 的进度。

## 诚实规则（本项目反复强调）

- 不假装看过图：缺原图、比例未标定、方位未知，都要写进 `assumptions`。
- 规范检查抓到的问题照实报，包括自己刚写的草案引入的错误。
- "逻辑连通" ≠ "空间已对接" ≠ "形态已确认" ≠ "玩法已验证"，不要用一个完成标记掩盖。
- 发现自己的旧结论错了，在 `design-log/` 里更正并写明原因，不要悄悄改。

## 常用命令

```bash
cd app-v2
npm run dev        # http://127.0.0.1:4174/
npx tsc -b         # 类型检查（新增 domain 文件要加进 tsconfig.node.json 的 include）
npx vitest run     # 全部测试
```

新增领域文件后若报 TS6307，把它加进 `app-v2/tsconfig.node.json` 的 `include` 数组。

编辑文件时先停掉 dev server：Vite 监听会锁定文件导致写入失败（EBUSY）。
